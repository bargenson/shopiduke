type Query {
    products(first: Int): ProductConnection
}

type ProductConnection {
    edges: [ProductEdge]
}

type ProductEdge {
    node: Product
}

type Product {
    id: ID
    title: String
    description: String
}
